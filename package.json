{
  "name": "cab-cam",
  "version": "1.0.0",
  "private": true,
  "description": "Node relay and dashboard for Raspberry Pi camera streams.",
  "scripts": {
    "start": "node server.js",
    "start:lan": "node server.js --no-ngrok",
    "check": "node --check server.js && node --check api/index.js",
    "deploy:vercel": "vercel --prod"
  },
  "dependencies": {
    "qrcode": "^1.5.4"
  },
  "engines": {
    "node": ">=18"
  }
}
